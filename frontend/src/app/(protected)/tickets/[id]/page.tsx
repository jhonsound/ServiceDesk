import TicketDetailView from "@/components/tickests/TicketDetailView";

// La página ahora solo se encarga de renderizar el componente de la vista
export default function TicketDetailPage() {
  return <TicketDetailView />;
}